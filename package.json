{
  "name": "ecommerce-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=liquidationport",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "postinstall": "npm run postinstall --workspace=liquidationport-api"
  },
  "devDependencies": {},
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "prisma": "^6.19.3"
  }
}
